services:
  - type: web
    name: my-web-service
    env: python
    pythonVersion: 3.9  # Update to the desired Python version
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py